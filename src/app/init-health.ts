/**
 * Server-side initialization for yt-dlp health monitoring
 * This runs once when the Next.js server starts
 */

import './api/health/init/route';

// Export empty object to make this a valid module
export {};
