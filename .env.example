# ═══════════════════════════════════════════════════════════
#  HivePlay Configuration
#  Copy this to .env.local for local development
#  Docker users: these are set via docker-compose.yml
# ═══════════════════════════════════════════════════════════

# Node Environment
NODE_ENV=production

# Redis Connection (required for audio caching)
REDIS_URL=redis://localhost:6379

# ── Snapcast Multi-Room Casting (optional) ──
# Set SNAPCAST_HOST to your Snapcast server IP to enable casting.
# Leave empty or remove to disable casting entirely.
# SNAPCAST_HOST=192.168.0.100
# SNAPCAST_TCP_PORT=4953
# SNAPCAST_PORT=1705

# ── yt-dlp ──
YTDLP_AUTO_UPDATE=true
YTDLP_UPDATE_CHECK_INTERVAL=86400

# Next.js Settings
NEXT_TELEMETRY_DISABLED=1

# YouTube Data API v3 key (optional — HivePlay works without it)
# NEXT_PUBLIC_YOUTUBE_API_KEY=your_api_key_here
